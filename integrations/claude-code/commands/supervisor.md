---
description: Show Claude Supervisor status and statistics
allowed-tools: Bash(claude-supervisor:*)
---

Run the shell command `claude-supervisor status` and show me its output: the
latest supervised session and the aggregate statistics (runs, resumes,
approvals, hours saved).

If `claude-supervisor` is not found, tell me to reopen my terminal / restart
Claude Code (it may have just been added to PATH), or to install it with
`pipx install claude-supervisor`.
